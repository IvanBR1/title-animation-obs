{
  "name": "title-animation-obs",
  "version": "1.1.0",
  "private": true,
  "description": "Panel Node para crear y emitir títulos animados en OBS.",
  "type": "commonjs",
  "scripts": {
    "build": "node src/build-static.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "npm run build && node --test"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
